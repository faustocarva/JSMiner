for file in *.txt; do
    count=$(grep -c "Checkout conflict" "$file")
    echo "$file: $count 'Checkout conflict' lines"
done

for file in *.txt; do
    count=$(grep -c "Checkout conflict" "$file")
    echo "$file: $count 'Exception:' lines"
done

for file in *.csv; do
    count=$(wc -l < "$file")
    echo "No arquivo $file, encontradas $count linhas"
done
